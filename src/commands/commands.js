function importAll(context) {
    LS.init(context)
    LS.import()
}

function exportAll(context) {
    LS.init(context)
    LS.import()
    LS.export()
}

function sorting(context) {
    LS.init(context)
    LS.correct()
    LS.sorting()
}